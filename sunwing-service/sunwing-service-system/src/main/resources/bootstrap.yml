spring:
  cloud:
    nacos:
      discovery:
        server-addr: 129.211.3.251:8848
      config:
        server-addr: 129.211.3.251:8848
        file-extension: yaml
        group: DEFAULT_GROUP
        enabled: true
  application:
    name: sunwing-service-system

server:
  port: 8001

